import { style } from "typestyle";

const mapContainer = style(
    {
        height: '100%',
        marginLeft: 'auto'
    }
);

const mainContainer = style({
    display: 'flex'
})

export default {
    mapContainer,
    mainContainer
}